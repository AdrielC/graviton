{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:quasar:schema:meta-envelope:v1.1:stored",
  "title": "Quasar Metadata Envelope v1.1 (stored/pinned form)",
  "type": "object",
  "additionalProperties": false,
  "required": ["system", "namespaces"],
  "properties": {
    "system": { "$ref": "#/$defs/system" },
    "namespaces": { "$ref": "#/$defs/namespacesStored" }
  },
  "$defs": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "system": {
      "type": "object",
      "additionalProperties": true,
      "required": ["envelope", "createdAt", "createdBy", "updatedAt", "source"],
      "properties": {
        "envelope": {
          "type": "string",
          "format": "iri",
          "const": "urn:quasar:meta-envelope:v1.1"
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "createdBy": {
          "type": "object",
          "additionalProperties": true,
          "required": ["principal"],
          "properties": {
            "principal": { "$ref": "#/$defs/nonEmptyString" }
          }
        },
        "source": {
          "type": "object",
          "additionalProperties": true,
          "required": ["requestId"],
          "properties": {
            "ingest": { "$ref": "#/$defs/nonEmptyString" },
            "requestId": { "$ref": "#/$defs/nonEmptyString" }
          }
        }
      }
    },
    "schemaRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["$id"],
      "properties": {
        "$id": { "type": "string", "format": "iri" }
      }
    },
    "errorDiagnostic": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "code", "message"],
      "properties": {
        "path": { "type": "string", "minLength": 1 },
        "code": { "type": "string", "minLength": 1 },
        "message": { "type": "string", "minLength": 1 }
      }
    },
    "namespaceEntryStored": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "status", "data"],
      "properties": {
        "schema": { "$ref": "#/$defs/schemaRef" },
        "status": {
          "$comment": "Stored envelopes must not persist the transient ingestion-only state 'unverified'.",
          "type": "string",
          "enum": ["valid", "quarantined"]
        },
        "data": { "type": "object" },
        "errors": {
          "$comment": "Diagnostics only; boundedness is an implementation concern.",
          "type": "array",
          "maxItems": 100,
          "items": { "$ref": "#/$defs/errorDiagnostic" }
        }
      }
    },
    "namespacesStored": {
      "type": "object",
      "propertyNames": { "type": "string", "format": "iri" },
      "additionalProperties": { "$ref": "#/$defs/namespaceEntryStored" }
    }
  }
}
