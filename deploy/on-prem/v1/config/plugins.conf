plugins {
  # v1 principle: ship exactly one end-to-end workflow.
  enabled = ["ocr"]

  ocr {
    # placeholder: container image / wasm module / command
    image = "REPLACE_ME/quasar-ocr-plugin:1.0.0"
    outputNamespace = "urn:tybera:cedar:ocr"
  }
}

