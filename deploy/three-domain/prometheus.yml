global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/graviton-recording-rules.yml
  - /etc/prometheus/graviton-alerts.yml

scrape_configs:
  - job_name: graviton
    static_configs:
      - targets: [graviton:8081]
